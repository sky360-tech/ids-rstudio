load_lab("1A")
data(cdc)
view(cdc)
View(cdc)
?cdc
dim(cdc) #This gives the dimension of the dataset
nrow(cdc) #This gives the number of rows/observations of the dataset
ncol(cdc) #This gives the number of colums/variables of the dataset
names(cdc) #This gives the names of the variables in the dataset
Names(cdc)
NAMES(cdc)
names(cdc)
names(CDC)
function(y ~ x, data = ___)
bargraph(~grade, data = cdc)
histogram(~height, data = cdc)
bargraph(~height, data = cdc)
xyplot(weight~height, data = cdc)
histogram(~weight, data = cdc)
bargraph(~eat_fruit, data = cdc)

nrow(cdc)

ncol(cdc)

dim(cdc)

names(cdc)

xyplot(age~height, data = cdc)

xyplot(weight~height, data = cdc)
xyplot(~height, data = cdc)

bargraph(~drink_milk, data = cdc)

bargraph(~drink_soda, data = cdc)
histogram(~weight, data = cdc)

xyplot(age~height,data=cdc)

bargraph(~drink_soda,data=cdc)
# reviewed: 2026-05-27 23:14:42 [2] run-14230
